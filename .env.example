# Frontend Environment Variables

# The URL of the backend API (used during server-side rendering/proxying)
# For local dev with Docker Compose: http://backend:8000
# For AWS deployment (separate services): https://api.yourdomain.com
# Default is http://127.0.0.1:8000
API_URL="http://127.0.0.1:8000"
